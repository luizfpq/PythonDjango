''' Exercicio resolucao exemplo '''
def maior_comprimento(**kwargs):
    maior = 0
    for chave in range(kwargs):
        if len(str(lista_str[i])) > len(str(lista_str[maior])):
            maior = i
    return lista_str[maior]

#print maior_comprimento(lista)